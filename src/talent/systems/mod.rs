pub mod apply;
// Merchant operations (remove-talent, 3-for-1 trade-up) are Phase 8 scaffold — defined so the
// event/flow shape is visible, but not yet wired or constructed. Silence dead-code until then.
#[allow(dead_code)]
pub mod merchant;
